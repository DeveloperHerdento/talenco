export type FAQKey = "eligibility" | "englishLevel" | "fee" | "deadline" | "format" | "certificate";

export const FAQ_KEYS: FAQKey[] = ["eligibility", "englishLevel", "fee", "deadline", "format", "certificate"];
